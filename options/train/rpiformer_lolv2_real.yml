name: rpiformer_lolv2_real
model_type: ImageCleanModel
scale: 1
num_gpu: 1
manual_seed: 100
use_amp: true

datasets:
  train:
    name: LOLv2RealTrain
    type: Dataset_PairedImage
    dataroot_gt: data/LOLv2/Real_captured/Train/Normal
    dataroot_lq: data/LOLv2/Real_captured/Train/Low
    geometric_augs: true
    filename_tmpl: "{}"
    io_backend:
      type: disk
    use_shuffle: true
    num_worker_per_gpu: 8
    batch_size_per_gpu: 8
    mini_batch_sizes: [8]
    iters: [150000]
    gt_size: 256
    gt_sizes: [256]
    dataset_enlarge_ratio: 1
    prefetch_mode: ~

  val:
    name: LOLv2RealVal
    type: Dataset_PairedImage
    dataroot_gt: data/LOLv2/Real_captured/Test/Normal
    dataroot_lq: data/LOLv2/Real_captured/Test/Low
    io_backend:
      type: disk

network_g:
  type: RPIFormer
  in_channels: 3
  out_channels: 3
  n_feat: 40
  stage: 1
  num_blocks: [1, 2, 2]
  lca_encoder_layers: []
  lca_decoder_layers: []
  use_lca_bottleneck: true
  downsample_lca: false
  lca_gate_init: 0.03
  lca_gate_scale: 0.10
  euler_cfg:
    enable_encoder_gate: true
    enable_decoder_gate: true
    encoder_init_alpha: 0.06
    decoder_init_alpha: 0.01
    max_gate_alpha: 0.3

path:
  pretrain_network_g: pretrained_weights/euler/best_model_LOL_v2_real.pth
  strict_load_g: false
  resume_state: ~

train:
  total_iter: 150000
  warmup_iter: 2000
  use_grad_clip: true
  scheduler:
    type: CosineAnnealingRestartCyclicLR
    periods: [60000, 90000]
    restart_weights: [1, 0.5]
    eta_mins: [0.00004, 0.000001]
  mixing_augs:
    mixup: false
    mixup_beta: 0.8
    use_identity: true
  optim_g:
    type: Adam
    lr: !!float 8e-5
    betas: [0.9, 0.999]
  pixel_opt:
    type: L1Loss
    loss_weight: 1
    reduction: mean

val:
  window_size: 4
  val_freq: 1000
  save_img: false
  rgb2bgr: true
  use_image: false
  max_minibatch: 8
  metrics:
    psnr:
      type: calculate_psnr
      crop_border: 0
      test_y_channel: false

logger:
  print_freq: 500
  save_checkpoint_freq: 2000
  use_tb_logger: false

dist_params:
  backend: nccl
  port: 29500
